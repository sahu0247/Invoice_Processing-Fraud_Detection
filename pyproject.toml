[project]
name = "invoice-fraud-detection"
version = "0.1.0"
description = "AI Invoice Fraud Detection Agent"
authors = [
    { name = "Sunayana Padhy" }
]

dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "faker",
    "openenv"
]

[project.scripts]
server = "app:app"
